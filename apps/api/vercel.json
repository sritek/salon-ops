{
    "version": 2,
    "installCommand": "cd ../.. && pnpm install",
    "buildCommand": "cd ../.. && pnpm --filter @salon-ops/shared build && pnpm --filter @salon-ops/api db:generate && pnpm --filter @salon-ops/api build",
    "builds": [
        {
            "src": "api/index.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/index.ts"
        }
    ]
}